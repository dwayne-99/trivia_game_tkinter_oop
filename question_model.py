class Question:

    # Define the constructor method for the Question class
    # This method is called when creating a new Question object
    def __init__(self, q_text, q_answer):

        # Create an instance variable 'text' and assign it the value of q_text
        self.text = q_text

        # Create an instance variable 'answer' and assign it the value of q_answer
        self.answer = q_answer
